%h2= @user.name
%p

	User since:
	=@user.created_at.strftime '%A %B %m %Y'


= image_tag @user.avatar

%ul
	- @user.mealplans.each do |mealplan|
		- mealplan.recipes.each do |recipe|
			%li= link_to recipe.name, recipe
